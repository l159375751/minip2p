# turnserver.conf (STUN-only, minimal)
stun-only
listening-port=3478
fingerprint

# Optional (if you want to bind explicitly):
# listening-ip=0.0.0.0
# listening-ip=::

# Optional logging:
# log-file=stdout
# verbose

